export default function createJobData(id) {
  return {
    [`company${id}`]: "",
    [`job-start${id}`]: "",
    [`job-end${id}`]: "",
    [`title${title}`]: "",
    [`responsibilities${id}`]: "",
  };
}
